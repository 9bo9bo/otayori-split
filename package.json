{
  "name": "otayori-split",
  "version": "1.2.1",
  "private": true,
  "type": "module",
  "description": "A3/B4のお便りPDFをA4×2・B5×2に分割する Scriptable × ショートカット用ツール",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build-scriptable.mjs",
    "test": "node src/paper.test.js && node scripts/split-smoke.mjs && npm run build && node scripts/scriptable-engine-smoke.mjs"
  },
  "dependencies": {
    "@cantoo/pdf-lib": "^2.8.1"
  }
}
